<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility service configuration for WhatsAppAccessibilityService.
    - canRequestFilterKeyEvents: allows the service to intercept hardware key events (volume buttons).
    - feedbackType: set to "spoken" because this service provides spoken feedback via TTS.
    - notificationTimeout: 100ms polling interval for accessibility events.
    - description: displayed to the user in the accessibility settings screen.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityFeedbackType="feedbackSpoken"
    android:accessibilityFlags="flagRequestFilterKeyEvents"
    android:canRequestFilterKeyEvents="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100" />
